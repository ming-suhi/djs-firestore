name: 🧪 SDK Extension Testing

on: [push, pull_request, workflow_dispatch]

env: ${{ secrets }}

jobs:

  GuildsManager:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Setup Node
      uses: actions/setup-node@v2
      
    - name: Install dependencies
      run: npm ci
    
    - name: get method
      run: node_modules\.bin\jest tests/guilds-manager/get
      
    - name: post method
      run: node_modules\.bin\jest tests/guilds-manager/post
      
    - name: delete method
      run: node_modules\.bin\jest tests/guilds-manager/delete
      
    - name: fetch method
      run: node_modules\.bin\jest tests/guilds-manager/fetch
