/**
 * @typedef {string} documentPath absolute path leading to document
 */

/**
 * @typedef {string} docID doc id
 */

/**
 * @typedef {object} docData doc field values
 * @property {docID} id doc id
 */

/**
 * @typedef {object} fieldValues doc field values
 */